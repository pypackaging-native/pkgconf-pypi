[build-system]
build-backend = 'hatchling.build'
requires = ['hatchling']

[project]
name = 'namespace'
version = '1.0.0'

[project.entry-points.pkg_config]
namespace = 'namespace'

[tool.hatch.build.targets.wheel]
packages = ['namespace']
